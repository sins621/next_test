
const nextConfig = {
  output: 'standalone',
  // Your other settings go here
}

module.exports = {
  basePath: '/test',
  assetPrefix: '/test/', // optional but helps with assets
}

export default nextConfig;
